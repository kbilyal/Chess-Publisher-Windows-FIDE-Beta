{
  "freeze": "Chess-Publisher unified SYNC",
  "version": "1.06.00-beta.96",
  "status": "PREPARED CANDIDATE — NOT ACTIVE UNTIL USER RUNTIME CONFIRMATION",
  "fingerprintContentSchema": 7,
  "policy": "These hashes are the repaired beta.96 r4 Windows/Web identity-aligned SYNC candidate contract. The SYNC FREEZE becomes active only after explicit user confirmation that real Desktop-Web/Cloud SYNC works. After activation, TEC/UI/release work must not modify these files without an explicitly reproduced SYNC defect and approval.",
  "files": {
    "webview/CloudWorkspaceAdapter.js": {
      "mode": "exact",
      "sha256": "5879e24f75f0879e4bb81ed882d4258f27a5aae0d2746e7c4ee5d9f38bd45ae4"
    },
    "cloud/client/cloud-workspace-api.js": {
      "mode": "exact",
      "sha256": "e69c84af5b9148ac6287f5e1e959310a10f2fcb28541740dd9c8dd4510e1e51a"
    },
    "webview/CloudWorkspaceRedesign.js": {
      "mode": "normalize-version",
      "sha256": "b2b0243d5934d595607680824edb98de21f044432c51c4bbcaeb4aee387be8d7"
    },
    "ChessPublisher-LocalEngine.ps1": {
      "mode": "exact",
      "sha256": "71113c2763ebfd438eab3dd3d4c1d8f11f630c59d67224c2d97e4568c8db3550"
    },
    "ChessPublisher-WebView.ps1": {
      "mode": "exact",
      "sha256": "9e28e7f456fe12697714c3ca27ea91cd71dbf11b324b3fe13cd75559e95cb810"
    }
  },
  "semanticGuards": [
    "checker provenance lastCheckerUiSource/lastCheckerUiBuildVersion/lastCheckerUiCheckedAt is runtime-only and excluded recursively from schema-7 fingerprints/private snapshots",
    "Windows launcher ChessPublisher-WebView.ps1 and ChessPublisher-LocalEngine.ps1 remain matched at serviceVersion V140; r4 does not change the proven r3 startup transport",
    "Windows Desktop Cloud transport uses the same-origin LocalEngine /cloud-proxy path and does not depend on Cloudflare browser CORS for 127.0.0.1/localhost",
    "LocalEngine Cloud proxy forwards only allowlisted Cloud workspace/tournament endpoints to the fixed Hub Worker host, requires Organizer bearer authorization, and preserves X-Expected-Revision",
    "Web production origin keeps the normal remote Cloud API path and WEB_ORIGIN policy",
    "Desktop identity selection matches current Web durable identity candidates and never selects a Cloud tournament by name alone",
    "an exact organizer-owned saved cloudTournamentId may repair only a generated/legacy Desktop internalId when no competing row owns that generated identity; meaningful conflicting identities remain fail-closed",
    "Cloud CREATE first checks exact organizer-owned stable identity and Cloud PUT preflights the selected row identity before guarded write",
    "every successful Desktop push is followed by an authoritative current-snapshot GET whose schema-7 fingerprint must equal the local content; pull is also verified after durable local apply",
    "cpCloudSyncCurrent remains the single unified SYNC authority",
    "no last-write-wins or silent stale overwrite",
    "revision guard and stable internalId/cloudTournamentId identity remain required",
    "results, next-round pairings, metadata/calendar, Trash restore, copied identity and common-base recovery regressions remain mandatory"
  ]
}
